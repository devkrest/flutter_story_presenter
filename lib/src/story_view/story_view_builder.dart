import 'package:flutter/material.dart';
class StoryViewBuilder extends StatelessWidget {
  const StoryViewBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
